export type ShiftStatus = "OPEN" | "CLOSED";
export interface CashShift { id: string; status: ShiftStatus; openingCash: number; openedAt: string; countedCash: number | null; expectedCash: number | null; variance: number | null; closedAt: string | null; note: string | null; }
export interface ShiftReport { from: string; to: string; shift: CashShift | null; summary: { cash: number; bankTransfer: number; customerConfirmation: number; legacy: number; paidOrders: number; paidRevenue: number; refunds: number; netRevenue: number; averageOrderValue: number; unpaidOrders: number; cancelledOrders: number }; transactions: Array<{ id: string; method: string; source: string; totalAmount: number; paidAt: string; receivedAmount: number | null; changeAmount: number | null }> }
